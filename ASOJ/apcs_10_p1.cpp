#include <iostream>
using namespace std;

int main() {
    int n, m, a, b, c;
    cin >> n >> m;
    int arr[m];
    for (int i = 0; i < n; i++) {
        cin >> a >> b >> c;
        if (a == 1) {
            
        }
    }

    return 0;
}